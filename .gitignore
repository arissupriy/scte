# ── Rust / Cargo ─────────────────────────────────────────────────────────────
/target/
**/target/
**/*.rs.bk
*.pdb

# Cargo.lock: tracked for reproducible builds (binary crates in workspace).
# Uncomment to ignore for library-only workspaces.
# Cargo.lock

# cargo-mutants
**/mutants.out*/

# cargo-fuzz artifacts
fuzz/artifacts/
fuzz/corpus/

# criterion benchmark output
criterion/

# ── WASM ─────────────────────────────────────────────────────────────────────
bindings/wasm/pkg/
bindings/wasm/target/

# ── C / FFI generated files ───────────────────────────────────────────────────
scte-ffi/include/*.generated.h
bindings/dart/lib/scte_bindings.g.dart

# ── Android / iOS (mobile FFI) ───────────────────────────────────────────────
*.aar
*.xcframework
*.a
*.so
*.dylib
*.dll

# ── Node.js ───────────────────────────────────────────────────────────────────
node_modules/
bindings/node/*.node
bindings/node/dist/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
dist/
bindings/python/build/

# ── IDEs ─────────────────────────────────────────────────────────────────────

# VS Code
.vscode/
!.vscode/extensions.json

# JetBrains (RustRover, IntelliJ, CLion)
.idea/
*.iml
*.iws
*.ipr

# Vim / Neovim
*.swp
*.swo
*~
.netrwhist

# Emacs
*\#
.\#*

# Eclipse
.classpath
.project
.settings/

# ── OS ────────────────────────────────────────────────────────────────────────

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~

# ── Environment & secrets ─────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx

# ── Test & benchmark artifacts ────────────────────────────────────────────────
*.scte.tmp
bench_output/

# ── Project-specific ─────────────────────────────────────────────────────────

# Design & research notes (not part of source)
desc*.txt

# Keep .github tracked (contains SKILL workflow)
# .github/

assets/*